import React from 'react';
import Layout from '../components/layout/Layout';

const Game = () => {
    return (
        <Layout>
            <div className="flex items-center justify-center h-full text-3xl font-orbitron text-white animate-pulse">
                GAME LOADING...
            </div>
        </Layout>
    );
};

export default Game;
